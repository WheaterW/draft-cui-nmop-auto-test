# draft-cui-nmop-auto-test

This repository tracks the Internet-Draft `draft-cui-nmop-auto-test`.

- The repository root contains the current `-00` draft source and generated outputs.
- The main source file in this repository is `draft-cui-nmop-auto-test-00.md`.

The XML, TXT, and PDF files are generated from the Markdown source using `kdrfc`.
